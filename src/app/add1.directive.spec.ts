import { Add1Directive } from './add1.directive';

describe('Add1Directive', () => {
  it('should create an instance', () => {
    const directive = new Add1Directive();
    expect(directive).toBeTruthy();
  });
});
